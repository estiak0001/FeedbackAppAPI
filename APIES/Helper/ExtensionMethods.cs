using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace APIES.Helper
{
    public static class ExtensionMethods
    {
        //public static IEnumerable<CoreUserInfo> WithoutPasswords(this IEnumerable<CoreUserInfo> users)
        //{
        //    if (users == null) return null;

        //    return users.Select(x => x.WithoutPassword());
        //}

        //public static CoreUserInfo WithoutPassword(this CoreUserInfo user)
        //{
        //    if (user == null) return null;

        //    user.Password = null;
        //    return user;
        //}
    }
}